[project]
name = "transformer-from-scratch"
version = "0.1.0"
description = "Autoregressive Transformer implementation"
dependencies = [
    "torch",
    "matplotlib",
    "jaxtyping",
    "requests",
    "numpy>=2.4.2",
]
